/*
 * Baekjoon Online Judge #{prob_no}
 * https://www.acmicpc.net/problem/{prob_no}
 */

#include <ios>
#include <iostream>

int main() {{
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);

  return 0;
}}
